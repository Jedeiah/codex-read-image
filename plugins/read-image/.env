# 视觉模型配置模板（把下面的值填成你自己的即可，改完重启 Codex 生效）
# 推荐把配置放到 ~/.config/read-image/.env（升级插件不会被覆盖），
# 或直接编辑本文件（插件目录内），脚本会优先读取前者。
# 不需要的项可以直接删掉或留空，脚本会使用默认值。

# 必填：视觉模型服务的 API 密钥（例如阿里云百炼、OpenAI、智谱、硅基流动等）
READ_IMAGE_API_KEY=

# 视觉模型名称。常用示例：
#   OpenAI:    gpt-4o-mini / gpt-4o
#   通义千问:   qwen-vl-max （配合下面的 DashScope 兼容地址）
#   智谱 GLM:   glm-4v-plus
READ_IMAGE_MODEL=qwen-vl-max

# OpenAI 兼容接口地址。通义千问示例：
READ_IMAGE_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1

# 注意：这个文件里是你的密钥，不要把它分享给别人。
