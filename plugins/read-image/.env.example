# 视觉模型配置模板。首次使用：把本文件复制为同目录下的 .env，再填下面的值。
# 例如：cp .env.example .env
# .env 不会进入 Git 仓库，插件升级不会覆盖你的配置；
# 插件卸载 / 移除市场时，.env 会随插件目录一起被删除。
# 不需要的项可以直接删掉或留空，脚本会使用默认值。
# 不填任何值时默认使用 gpt-4o-mini + https://api.openai.com/v1。

# 必填：视觉模型服务的 API 密钥（例如阿里云百炼、OpenAI、智谱、硅基流动等）
READ_IMAGE_API_KEY=

# 视觉模型名称。常用示例：
#   OpenAI:    gpt-4o-mini / gpt-4o
#   通义千问:   qwen-vl-max （配合下面的 DashScope 兼容地址）
#   智谱 GLM:   glm-4.6v-flash / glm-4v-flash / glm-4v-plus
READ_IMAGE_MODEL=glm-4.6v-flash

# OpenAI 兼容接口地址。智谱示例：
READ_IMAGE_BASE_URL=https://open.bigmodel.cn/api/paas/v4

# 思考模式：auto（glm 系列自动开启，其他模型关闭）/ on / off
READ_IMAGE_THINKING=auto

# 注意：这个文件里是你的密钥，不要把它分享给别人。
